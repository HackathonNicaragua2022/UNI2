import { View, TextInput, Text } from "react-native";
import { colors } from "../colors";

export default function () {
  return (
    <View>
      <Text>Aqui son los ajustes</Text>
    </View>
  );
}
const makeStyles = (color) => {
  return StyleSheet.create({
    container: {},

    textStyle: {},
  });
};
